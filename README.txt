Pre-requisitos: NodeJS

Steps:
1. Create folder "test"
2. Run in cmd "npm init -y" and then "npm i -D webpack"
3. Add key and valu in scripts {} "/package.json"
	"dev": "node_modules/.bin/webpack index.js bundle.js"
4. Generate configuration file "webpack.config.js"
5. Create constant "const path = require('path');"
	Path > It serves for the routing of files in different operating systems

CMD: In your folder proyect run "npm run dev"
	dev > Step 3
